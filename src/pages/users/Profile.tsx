import React from "react";
// import { useGetUserProfileQuery } from "../store/api/userApi";


const Profile:React.FC = () => {
  // Replace with authenticated user's ID
  // const user_id = 1;

  // const { data, isLoading, isError } = useGetUserProfileQuery(user_id);

  // if (isLoading) return <div className="p-6 text-center text-gray-500">Loading profile...</div>;
  // if (isError) return <div className="p-6 text-center text-red-500">Error loading profile.</div>;

  // const user = data?.user;

  return (
//     <div className="max-w-4xl mx-auto p-6">
//       <h1 className="text-3xl font-semibold mb-6 text-gray-800">My Profile</h1>

//       <div className="grid md:grid-cols-2 gap-6">
//         {/* Profile Overview Card */}
//         <div className="bg-white shadow-lg rounded-2xl p-6 border border-gray-200">
//           <h2 className="text-xl font-semibold mb-4 text-gray-700">Personal Details</h2>

//           <div className="space-y-3">
//             <Field label="First Name" value={user?.first_name} />
//             <Field label="Last Name" value={user?.last_name} />
//             <Field label="Email" value={user?.email} />
//             <Field label="Phone" value={user?.phone || "Not provided"} />
//             <Field label="Country" value={user?.country} />
//             <Field label="Language" value={user?.language} />
//             <Field label="Time Zone" value={user?.time_zone} />
//           </div>
//         </div>

//         {/* Rental Data Card */}
//         <div className="bg-white shadow-lg rounded-2xl p-6 border border-gray-200">
//           <h2 className="text-xl font-semibold mb-4 text-gray-700">Rental Information</h2>

//           <div className="space-y-3">
//             <Field label="Total Rentals" value={user?.stats?.total_rentals ?? 0} />
//             <Field label="Active Bookings" value={user?.stats?.active_bookings ?? 0} />
//             <Field label="Completed Rentals" value={user?.stats?.completed ?? 0} />
//             <Field label="License Number" value={user?.license || "Not provided"} />
//             <Field label="Preferred Vehicle Type" value={user?.preferences?.vehicle_type || "None"} />
//           </div>
//         </div>
//       </div>
//     </div>
//   );
// };

// /** Reusable read-only field component */
// const Field = ({ label, value }) => (
//   <div>
//     <p className="text-sm text-gray-500">{label}</p>
//     <div className="mt-1 w-full bg-gray-100 border border-gray-300 rounded-xl p-2 text-gray-800 text-sm">
//       {value}
//     </div>
//   </div>
<div></div>
);
}
export default Profile;
